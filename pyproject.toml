[project]
name = "nexback"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["pyside6>=6.10.1"]

[project.optional-dependencies]
scripts = ["edge-tts>=7.2.3"]
